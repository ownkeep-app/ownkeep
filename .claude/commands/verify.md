---
description: Legacy wrapper for the OwnKeep /verify project skill.
argument-hint: "[optional target or acceptance criteria]"
---

Claude Code now includes a bundled `/verify` skill, so OwnKeep defines the active project-level
override at `.claude/skills/verify/SKILL.md`.

Follow that skill exactly for this invocation:

@.claude/skills/verify/SKILL.md
